import { CwDirective } from './cw.directive';

describe('CwDirective', () => {
  it('should create an instance', () => {
    const directive = new CwDirective();
    expect(directive).toBeTruthy();
  });
});
